# Copy this file to .env and fill in your credentials and settings.  Never
# commit your real secrets to version control.

## Shopify configuration
# Webhook secret used to verify that requests are genuinely from Shopify.  You
# can find this in the Shopify admin under Settings → Notifications → Webhooks
# or inside your custom app when you create a webhook.  It will be shown
# when you add the webhook.
SHOPIFY_WEBHOOK_SECRET=shpss_xxx

# Domain of your Shopify store without https://.  For example
# mystore.myshopify.com
SHOPIFY_STORE_DOMAIN=x0rdyt-up.myshopify.com

# Admin API token from your custom app.  When you create the app you will
# generate this token under API Credentials.
SHOPIFY_ADMIN_TOKEN=shpat_xxx

## Honey's Place configuration
# Your Honey's Place account number.  This is shown under My Account → Data
# Integration Services → API Setup.
HP_ACCOUNT=01234

# Your Honey's Place API token (password).  Also found under API Setup.
HP_TOKEN=P@ssw0rd

# Default shipping code.  Recommended value is RTSHOP to let Honey's choose the
# best rate.  Other examples: P002 (USPS Priority), F006 (FedEx Ground),
# PICKUP (Customer pickup).
HP_DEFAULT_SHIP=RTSHOP

## Polling interval
# Interval (in minutes) between calls to the Honey's Place order status API
# to fetch tracking numbers.  15 minutes is a reasonable value.  Increase
# this if you want fewer API calls.
POLL_INTERVAL_MINUTES=15

## Server configuration
# Port for the Express server.  Defaults to 3000 if not specified.
PORT=3000